spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/tutorial?UseSSL=false
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:Minion1027$}
spring.jpa.hibernate.ddl-auto=create
server.port=8082

// use these variables in - run-configuration -variables : 
// -DMYSQL_USER=root
// -DMYSQL_PASSWORD=root
// -DMYSQL_PORT=3307
// while running it for docker(CMD) or creating docker image